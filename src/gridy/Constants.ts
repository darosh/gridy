/// <reference path="Float.ts" />

module Gridy {
    'use strict';

    export module Constants {
        'use strict';

        export var SQRT_3:Float = Math.sqrt(3);
        export var SQRT_3_2:Float = Math.sqrt(3) / 2;
        export var SQRT_3_3:Float = Math.sqrt(3) / 3;
    }
}
